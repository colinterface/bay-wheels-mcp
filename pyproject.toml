[project]
name = "bay-wheels-mcp"
version = "0.1.0"
description = "MCP server for Bay Wheels bike share system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "geopy>=2.4.1",
    "httpx>=0.28.1",
    "mcp>=1.23.1",
    "starlette>=0.27.0",
    "uvicorn>=0.30.0",
]
